<header class="header flex justify-between items-center px-6 py-3 bg-green-600">
  <a href="/html/product" class="flex items-center gap-2">
    <img src="/images/logo.png" alt="KazABurger" class="h-12 w-auto">
    <span class="text-2xl font-bold text-yellow-300">KazABurger</span>
  </a>

  <nav class="flex items-center gap-6">
    <% if (user?.role === 'admin') { %>
      <a href="/html/suggest" class="nav-text">Suggestions</a>
    <% } %>

    <% if (user) { %>
      <span class="text-yellow-300 text-sm">Bonjour <%= user.username %></span>
      <a href="/logout" class="nav-text">Se déconnecter</a>
    <% } else { %>
      <a href="/login" class="nav-text">Se connecter</a>
    <% } %>
  </nav>
</header>
